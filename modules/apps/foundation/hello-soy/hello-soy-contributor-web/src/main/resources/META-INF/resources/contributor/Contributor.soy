{namespace Contributor}

/**
 * Prints the Hello Soy portlet footer.
 *
 * @param id
 * @param test
 */
{template .render}
	<div id="{$id}">
		<blockquote class="blockquote-xs">
			<small>Contributed {$test}</small>
		</blockquote>
	</id>
{/template}